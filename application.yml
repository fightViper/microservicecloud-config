spring:
 profiles:
   active:
   -dev

spring:
 profiles: dev
 application:
   name: microservicecloud-config-langou-dev

spring:
 profiles: test #测试环境
 application:
   name: microservicecloud-config-langou-test

#保存为UTF-8格式